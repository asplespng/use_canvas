= simple_form_for(@movement) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :kind, collection: Movement.kinds.keys, label_method: 'humanize'
    = f.input :created_at, as: :date_picker

  .form-actions
    = f.button :submit
